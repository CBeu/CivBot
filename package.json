{
  "name": "discordbot",
  "version": "1.0.0",
  "description": "A bot that will read messages sent from a webhook and mention the user in discord",
  "main": "main.js",
  "scripts": {
    "start": "node main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Craig Beuerlein",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^12.2.0",
    "request": "2.81.0"
  }
}
